struct Point {
    int x;
    int y;
};
int f(int x, bool b) {

	x.n = 2;
	p.z = 3;

}

void g() {
    int a;
    bool b;
    struct Point p;

    bool b;

    struct Ponyta pon;

    p.x = c;


    b = a == 3;
    f(a + p.y*2, b);
    g();
}
