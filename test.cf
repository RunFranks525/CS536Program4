int x;
int y;
bool z;
struct myStruct { int x; int y; };

int function(int w) {
	return 5;
}

bool loopyFunc(int c) {
	if(c > 5) {
		int fifty;
		fifty = 50;
	} else {
		int fifty;
		fifty = 50;
	}
	return false;
}

int outOfScope(int outOfScope){
	
}

void grabMeSomeGlobals(int x, bool z) {}

void voidFunc(){
	int y;
	y = 50;
	if(y > 10) {
		int y;
		y = 10;
	}
	while(y > 40){
		y--;
	}
}

int allowDuplicateName() {
	int allowDuplicateName;
}

bool anotherFunction(int b, int d) {

	int q;

	if(b < 6) {
		b = 10;
	}	
	else {
		b = 4;
	}
	return loopyFunc(c < -4);
}
